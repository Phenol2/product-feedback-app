import { createSlice } from "@reduxjs/toolkit"

let initialState = {

}

let productSlice = createSlice({
  name: "product",
  initialState,
  reducers:{

  },
})

export default productSlice